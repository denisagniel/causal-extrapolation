% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_score_cov.R
\name{estimate_score_cov}
\alias{estimate_score_cov}
\title{Covariance of a joint cell score matrix, with optional shrinkage}
\usage{
estimate_score_cov(
  score_matrix,
  shrink = c("auto", "none", "diagonal", "ledoit-wolf"),
  alpha = NULL,
  min_ratio = 10
)
}
\arguments{
\item{score_matrix}{An \eqn{n_{\text{eff}} \times J} numeric matrix of per-unit (or
per-cluster) cell scores, as returned by \code{\link[=build_score_matrix]{build_score_matrix()}}.}

\item{shrink}{Shrinkage rule; one of \code{"auto"}, \code{"none"}, \code{"diagonal"}, \code{"ledoit-wolf"}.
See Shrinkage above.}

\item{alpha}{Optional shrinkage intensity in \eqn{[0, 1]}, overriding the rule implied by
\code{shrink}. Supplying \code{alpha} with \code{shrink = "auto"} pins the intensity while leaving the
automatic fallback logic in place.}

\item{min_ratio}{Positive floor on \eqn{n_{\text{eff}}/J}. Below it, \code{shrink = "auto"}
warns and shrinks rather than attempting an ill-conditioned inverse. Default \code{10}.}
}
\value{
A list with
\describe{
\item{\code{Sigma}}{the \eqn{J \times J} (possibly shrunk) covariance, always returned.}
\item{\code{Sigma_inv}}{its inverse, or \code{NULL} when inversion is unreliable.}
\item{\code{shrink_used}}{the rule actually applied (\code{"auto"} is never reported; the
resolved rule is).}
\item{\code{alpha}}{the shrinkage intensity used.}
\item{\code{kappa}}{condition number of \code{Sigma}.}
\item{\code{rank}}{numerical rank of \code{Sigma}.}
\item{\code{n_eff}, \code{J}}{dimensions of \code{score_matrix}.}
\item{\code{ok}}{\code{TRUE} iff \code{Sigma} is full rank and \code{Sigma_inv} was computed.}
}
}
\description{
Estimates the \eqn{J \times J} covariance \eqn{\Sigma} of the per-\eqn{(g,t)}-cell scores
assembled by \code{\link[=build_score_matrix]{build_score_matrix()}}, together with its inverse and explicit conditioning
diagnostics. The covariance is on the package's per-unit score scale: the covariance
matrix of the cell \emph{estimates} is \eqn{\Sigma / n_{\text{eff}}}, matching
\code{\link[=compute_variance]{compute_variance()}}'s \eqn{\mathrm{Var}(\phi)/n} convention, and
\eqn{\Sigma_{jj} / n_{\text{eff}}} reproduces cell \eqn{j}'s usual variance exactly.
}
\section{Conditioning}{

With \eqn{J} cells and only \eqn{n_{\text{eff}}} independent units (or clusters), the
unshrunk sample covariance is exactly singular once \eqn{J \ge n_{\text{eff}}} and badly
conditioned well before that; cells that share most of their units are close to
collinear. This function never returns a confidently-wrong inverse: if \eqn{\Sigma}
cannot be inverted reliably it returns \code{Sigma_inv = NULL} and \code{ok = FALSE} with a
warning, leaving the decision to the caller.
}

\section{Shrinkage}{

All non-trivial options apply the same convex combination toward the diagonal target,
\deqn{\Sigma = (1 - \alpha)\widehat\Sigma + \alpha \, \mathrm{diag}(\widehat\Sigma),}
which leaves every cell's marginal variance untouched (the diagonal is identical for all
\eqn{\alpha}) and only damps the off-diagonal covariances. The options differ solely in
how \eqn{\alpha} is chosen:
\describe{
\item{\code{"none"}}{\eqn{\alpha = 0}: the raw sample covariance.}
\item{\code{"diagonal"}}{\eqn{\alpha = 1}: all cross-cell covariance discarded. This is the
implicit assumption of the package's historical list-of-columns representation.}
\item{\code{"ledoit-wolf"}}{\eqn{\alpha = \min(1, (J + 1) / n_{\text{eff}})} unless \code{alpha}
is supplied. \strong{This is a simplified variant, not the Ledoit-Wolf estimator.}
The genuine Ledoit-Wolf rule derives \eqn{\alpha} from an estimate of the optimal
(Frobenius-risk-minimizing) shrinkage intensity, which requires fourth-moment
quantities this function does not compute. What is implemented is a deterministic
dimension-to-sample-size rule: it is monotone in \eqn{J/n_{\text{eff}}}, reaches full
diagonal shrinkage exactly where the sample covariance becomes singular
(\eqn{J + 1 \ge n_{\text{eff}}}), and guarantees a positive-definite result whenever
every cell has positive variance — but it makes \emph{no} optimality claim. The name
is retained only so the option is findable; treat it as "automatic convex shrinkage".}
\item{\code{"auto"} (default)}{\code{"none"} when \eqn{n_{\text{eff}}/J \ge} \code{min_ratio};
otherwise warn and fall back to \code{"ledoit-wolf"}.}
}
}

\examples{
set.seed(20260925)
Phi <- matrix(rnorm(300 * 4), nrow = 300)
res <- estimate_score_cov(Phi)
res$shrink_used
res$ok

# Too few units relative to cells: auto shrinks and says so.
Phi_wide <- matrix(rnorm(40 * 15), nrow = 40)
suppressWarnings(estimate_score_cov(Phi_wide)$alpha)

}
\seealso{
\code{\link[=build_score_matrix]{build_score_matrix()}}
}
