% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrate_cate.R
\name{.apply_two_sample_variance}
\alias{.apply_two_sample_variance}
\title{Add the two-sample variance correction when the target is an external sample}
\usage{
.apply_two_sample_variance(core, tau, n, n_target, form, level)
}
\arguments{
\item{core}{Output of \code{.cate_core()} at the final (possibly truncated) weights.}

\item{tau}{Length-n conditional ATT predictions.}

\item{n}{Source sample size.}

\item{n_target}{Size of the external target sample, or \code{NULL}.}

\item{form}{\code{"target_index"} or \code{"density_ratio"} (from \code{\link[=resolve_transport_weights]{resolve_transport_weights()}}).}

\item{level}{Confidence level.}
}
\value{
List with \code{se}, \code{ci}, \code{var}, \code{diag} (\code{NULL} unless the addendum applies).
}
\description{
Implements the additive variance correction of the two-sample transport proposition:
\eqn{V = V_{\mathrm{src}} + \rho\,\mathrm{Var}_Q(\tau)}, with \eqn{\widehat\rho = n/n^*}
and \eqn{\mathrm{Var}_Q(\tau)} the weighted variance of \code{tau} (via \code{.weighted_var_q()})
under the (possibly truncated) final weights. Only applies to Form B (\code{weights}); a
no-op with a warning for Form A, where the target is a fixed subsample of the source
and there is no second sample.
}
\keyword{internal}
