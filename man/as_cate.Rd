% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_cate.R
\name{as_cate}
\alias{as_cate}
\alias{as_cate.causal_forest}
\alias{as_cate.DoubleML}
\alias{as_cate.rlearner}
\alias{as_cate.drdid}
\alias{as_cate.default}
\title{Build a CATE contract from a fitted learner}
\usage{
as_cate(fit, ...)

\method{as_cate}{causal_forest}(fit, mu0 = NULL, mu1 = NULL, ...)

\method{as_cate}{DoubleML}(fit, ...)

\method{as_cate}{rlearner}(fit, X, A, Y, ...)

\method{as_cate}{drdid}(fit, ...)

\method{as_cate}{default}(fit, ...)
}
\arguments{
\item{fit}{A fitted learner object (e.g. from \code{grf::causal_forest()},
\code{DoubleML::DoubleMLIRM}, \code{rlearner}, or a \code{drdid} fit), or any object with a
registered \code{as_cate} method.}

\item{...}{Additional arguments passed to methods (e.g. explicit \code{mu0}/\code{mu1} overrides
for the grf method).}

\item{mu0, mu1}{Optional explicit outcome-regression predictions (length n). If omitted,
the grf method recovers them from the forest's local-centering identity
\eqn{\hat\mu_1 = \hat Y + (1 - \hat W)\hat\tau}, \eqn{\hat\mu_0 = \hat Y - \hat W\hat\tau},
which assumes grf's internal centering.}

\item{X}{Covariate frame at which to predict the CATE (required for the rlearner
method, which needs prediction points). Also stored in the contract.}

\item{A, Y}{Treatment (0/1) and outcome vectors (length n) accompanying \code{X}.}
}
\value{
A named list (the CATE contract; see \code{\link[=integrate_cate]{integrate_cate()}}) carrying a \code{design}
attribute (\code{"unconfoundedness"} or \code{"did"}).
}
\description{
Thin adapters that extract the predictions \code{\link[=integrate_cate]{integrate_cate()}} needs from a fitted CATE
learner. These are \strong{pure extractors}: they call the fitted object's own prediction
machinery and reshape the results into the generic contract list; they never refit a
model. Learner packages are optional (Suggests) — an adapter errors informatively if
the backing package is not installed.
}
\seealso{
\code{\link[=integrate_cate]{integrate_cate()}} for the contract definition and downstream use.
}
