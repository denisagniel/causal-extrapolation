% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_model_specs.R
\name{build_model_specs}
\alias{build_model_specs}
\title{Build Model Specifications for Cross-Validation}
\usage{
build_model_specs(model_names = c("linear", "quadratic"), custom_models = NULL)
}
\arguments{
\item{model_names}{Character vector of model names to include. Options:
\itemize{
\item \code{"linear"} - Linear trend extrapolation (requires p >= 2)
\item \code{"quadratic"} - Quadratic trend extrapolation (requires p >= 3)
}}

\item{custom_models}{Optional named list of custom model specifications to
append. Each element must be a list with fields: \code{h_fun}, \code{dh_fun}, \code{name}.}
}
\value{
A named list of model specifications suitable for \code{\link[=cv_extrapolate_ATT]{cv_extrapolate_ATT()}}.
}
\description{
Factory function to construct properly formatted model specifications for
time-series cross-validation. Simplifies creating candidate model lists by
providing common temporal extrapolation models (linear, quadratic, spline).
}
\details{
This function creates a named list of model specifications suitable for use
with \code{\link[=cv_extrapolate_ATT]{cv_extrapolate_ATT()}}. Each specification contains:
\itemize{
\item \code{h_fun} - Function factory: \verb{function(times, future_time)} returns a
function that maps observed ATTs to extrapolated values
\item \code{dh_fun} - Jacobian function: \verb{function(times, future_time)} returns
a numeric vector of partial derivatives for EIF propagation
\item \code{name} - Character name for display in results
}

See \code{\link[=hg_linear]{hg_linear()}} and \code{\link[=hg_quadratic]{hg_quadratic()}} for the interface design rationale.
}
\examples{
# Build linear and quadratic models
models <- build_model_specs(c("linear", "quadratic"))
names(models)  # "linear", "quadratic"

# Build only linear model
models_linear <- build_model_specs("linear")

# Add a custom model
custom <- list(
  constant = list(
    h_fun = function(times, future_time) {
      function(tau_g) mean(tau_g)
    },
    dh_fun = function(times, future_time) {
      rep(1 / length(times), length(times))
    },
    name = "constant"
  )
)
models_with_custom <- build_model_specs("linear", custom_models = custom)

}
