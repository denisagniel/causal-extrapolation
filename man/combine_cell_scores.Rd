% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_cell_scores.R
\name{combine_cell_scores}
\alias{combine_cell_scores}
\title{GMM/GLS-efficient combination of a joint cell score matrix}
\usage{
combine_cell_scores(
  score_matrix,
  values,
  restriction = NULL,
  target = NULL,
  cov = NULL,
  shrink = "auto",
  fallback = c("diagonal", "error"),
  omega_scores = NULL
)
}
\arguments{
\item{score_matrix}{An \eqn{n_{\text{eff}} \times J} numeric score matrix, as returned by
\code{\link[=build_score_matrix]{build_score_matrix()}}.}

\item{values}{Numeric vector of length \eqn{J}, the per-cell point estimates
\eqn{\widehat\theta} that \code{score_matrix}'s columns are the influence functions of.}

\item{restriction}{Either a known \eqn{J \times q} restriction matrix \eqn{R}, or a
length-\eqn{J} grouping vector (factor, character, or numeric) that is expanded to an
indicator matrix via \code{\link[=.build_restriction]{.build_restriction()}}. \code{NULL} (default) means every cell shares
one common scalar (\eqn{R} is a column of ones, \eqn{q = 1}).}

\item{target}{Numeric vector of length \eqn{q}, the coefficients \eqn{c} defining the
target \eqn{\psi = c'\beta}. \code{NULL} is only valid when \eqn{q = 1} (the implicit
\code{target = 1}, i.e. \eqn{\psi = \beta}).}

\item{cov}{Optional, the output of \code{\link[=estimate_score_cov]{estimate_score_cov()}} for \code{score_matrix}. Computed
internally via \code{estimate_score_cov(score_matrix, shrink = shrink)} when \code{NULL}.}

\item{shrink}{Shrinkage rule passed to \code{\link[=estimate_score_cov]{estimate_score_cov()}} when \code{cov} is \code{NULL}.
Ignored when \code{cov} is supplied. \strong{Used only to select the weights} \code{lambda}; the
reported standard error always comes from the realized, unshrunk variance of
\code{score_matrix \%*\% lambda} (see Details), so shrinkage never makes the reported SE
optimistic.}

\item{fallback}{What to do when \code{cov$ok} is \code{FALSE} (an unreliable inverse). \code{"diagonal"}
(default) recomputes with \code{shrink = "diagonal"} (always invertible) and warns.
\code{"error"} refuses instead.}

\item{omega_scores}{Optional \eqn{n_{\text{eff}} \times q} matrix of per-unit influence
functions for an \emph{estimated} \code{target}, as returned by \code{\link[=build_omega_score]{build_omega_score()}}.
Supply this only when \code{target} was itself estimated from this sample; the returned
\code{phi} then additionally carries the functional-delta-method term
\eqn{\sum_k \widehat\beta_k \phi_{c_k,i}}, exactly as \code{\link[=aggregate_groups]{aggregate_groups()}} does for
Path 1's cohort weights. The point estimate never depends on \code{omega_scores}.}
}
\value{
A list of class \code{"cell_combination"}:
\describe{
\item{\code{value}}{the combined point estimate \eqn{\widehat\psi}.}
\item{\code{phi}}{length-\eqn{n_{\text{eff}}} propagated influence function (plus the
\code{omega_scores} term, if supplied).}
\item{\code{lambda}}{length-\eqn{J} combination weights, named by \code{colnames(score_matrix)}.}
\item{\code{beta}, \code{vcov_beta}}{\eqn{\widehat\beta} and \eqn{A^{-1}/n_{\text{eff}}}.}
\item{\code{phi_beta}}{\eqn{n_{\text{eff}} \times q} matrix of per-free-parameter
influence functions of \eqn{\widehat\beta} (column \eqn{k} is the EIF of
\eqn{\widehat\beta_k}); \code{phi} is \code{phi_beta \%*\% target} up to the \code{omega_scores}
term.}
\item{\code{se}}{honest standard error from the realized, unshrunk variance of
\code{score_matrix \%*\% lambda} -- valid regardless of \code{shrink}.}
\item{\code{se_nominal}}{\eqn{\sqrt{c'A^{-1}c / n_{\text{eff}}}} from the (possibly
shrunk) \code{Sigma} used to choose \code{lambda}; a diagnostic only, not for inference.}
\item{\code{weighting_used}}{\code{"full-covariance"} or \code{"diagonal"} (see \code{fallback}).}
\item{\code{shrink_used}, \code{alpha}, \code{kappa}, \code{ok}}{passed through from \code{\link[=estimate_score_cov]{estimate_score_cov()}}.}
\item{\code{diagnostics}}{list with \code{l1_norm}, \code{min_lambda}, \code{n_negative}.}
}
}
\description{
Combines the columns of a (possibly correlated) score matrix into one or more target
linear combinations, using the full cross-cell covariance recovered by
\code{\link[=build_score_matrix]{build_score_matrix()}}/\code{\link[=estimate_score_cov]{estimate_score_cov()}} rather than assuming the cells are
independent. This is the natural generalization of \code{\link[=gls_weights]{gls_weights()}}, which uses only
the diagonal (per-cell variances) of that covariance.
}
\section{What this computes}{

Let \eqn{\Phi} be the supplied \eqn{n_{\text{eff}} \times J} score matrix with covariance
\eqn{\Sigma}, and \eqn{\widehat\theta} the corresponding length-\eqn{J} cell estimates
(\code{values}). Given a known restriction \eqn{\theta = R\beta} (\eqn{R} an \eqn{J \times q}
matrix; e.g. \eqn{q} groups, one column per group indicating which cells belong to it)
and a target \eqn{\psi = c'\beta} (\eqn{c} length \eqn{q}), the minimum-distance /
optimal-GMM combination is
\deqn{A = R'\Sigma^{-1}R, \qquad \lambda^* = \Sigma^{-1}R A^{-1}c, \qquad
  \widehat\beta = A^{-1}R'\Sigma^{-1}\widehat\theta, \qquad
  \widehat\psi = c'\widehat\beta = {\lambda^*}'\widehat\theta,}
with propagated influence function \eqn{\phi_\psi = \Phi\lambda^*}. By construction
\eqn{{\lambda^*}'R = c'} (unbiasedness), and when the same \eqn{\Sigma} is used
throughout, \eqn{{\lambda^*}'\Sigma\lambda^* = c'A^{-1}c} exactly.
}

\section{What efficiency this does and does not deliver}{

\code{combine_cell_scores()} returns the minimum-variance combination \strong{within the
class of fixed linear combinations of the supplied cell scores} that is unbiased for
the target: over all \eqn{\lambda} with \eqn{\lambda'R = c'}, it minimizes
\eqn{\mathrm{Var}(\lambda'\phi_i)}, attaining \eqn{c'A^{-1}c}. This is the optimal-GMM
(efficient minimum-distance) weighting for the linear moment system
\eqn{\E[\widehat\theta - R\beta] = 0}.

This is \strong{not} the semiparametric efficiency bound for the target functional, and
should not be described as such. It is efficient within the span of the \eqn{J} supplied
scores; attaining the bound additionally requires (i) that the efficient influence
function under the \emph{restricted} model lie in that span -- imposing a restriction
shrinks the tangent space, and the restricted EIF is a projection that need not be a
linear combination of the unrestricted cell EIFs; (ii) that the set of cells and the
first-stage comparison-group design already be optimal (e.g. \code{control_group = "notyettreated"} vs. \code{"nevertreated"} in a DiD first stage yields a different span and a
different attainable variance); and (iii) that the supplied per-cell scores already be
efficient for their own cell parameters -- efficiency is inherited from the first stage,
not created here.
}

\section{A practical caveat}{

When cells reuse comparison units (e.g. group-time ATTs sharing a not-yet-treated
comparison group), \eqn{\Sigma} is strongly positively correlated and can be
near-singular. The GLS optimum then legitimately takes large, opposite-signed entries in
\eqn{\lambda^*} -- nominally efficient, but finite-sample unstable and dominated by
whichever small cohort has the least-shared comparators. \code{diagnostics} flags this rather
than silently returning an unstable combination; consider a larger \code{alpha} (more
shrinkage) or \code{shrink = "diagonal"} if it fires.
}

\examples{
set.seed(20260925)
Phi <- matrix(rnorm(300 * 4), nrow = 300)
res <- combine_cell_scores(Phi, values = c(1, 1.2, 0.9, 1.1))
res$value
res$se

}
\seealso{
\code{\link[=build_score_matrix]{build_score_matrix()}}, \code{\link[=estimate_score_cov]{estimate_score_cov()}}, \code{\link[=gls_weights]{gls_weights()}} (the
diagonal-only special case this generalizes), \code{\link[=path1_aggregate]{path1_aggregate()}} (\code{weighting = "gls-joint"}).
}
