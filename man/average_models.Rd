% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/average_models.R
\name{average_models}
\alias{average_models}
\title{Model-Averaged Extrapolation with Exponential Weights}
\usage{
average_models(
  cv_result,
  gt_object,
  future_value,
  time_scale = c("calendar", "event"),
  temperature = 1,
  omega = NULL,
  per_group = TRUE,
  ...
)
}
\arguments{
\item{cv_result}{A \code{cv_extrapolate} object from \code{\link[=cv_extrapolate_ATT]{cv_extrapolate_ATT()}}.}

\item{gt_object}{The \code{gt_object} used in the original CV (from
\code{\link[=estimate_group_time_ATT]{estimate_group_time_ATT()}}).}

\item{future_value}{Numeric scalar: the future time to extrapolate to.}

\item{time_scale}{One of \code{"calendar"} or \code{"event"}; must match cv_result.}

\item{temperature}{Numeric scalar controlling weight concentration (default 1).
Weights are \verb{w_m ∝ exp(-MSPE_m / temperature)}. Lower temperature = more
concentration on best model; higher temperature = more uniform weights.}

\item{omega}{Optional group weights for aggregation. If provided, returns
aggregated estimate; if \code{NULL}, returns per-group estimates.}

\item{per_group}{Logical; if \code{TRUE}, return per-group results. If \code{FALSE},
aggregate across groups (requires \code{omega}).}

\item{...}{Additional arguments passed to model h_fun and dh_fun.}
}
\value{
An \code{extrap_object} with model-averaged estimates and EIFs. Contains:
\itemize{
\item \code{tau_g_future}: Per-group averaged estimates
\item \code{phi_g_future}: Per-group averaged EIFs
\item \code{tau_future}, \code{phi_future}: Aggregated estimates (if \code{per_group = FALSE})
\item \code{weights}: Named vector of model weights
\item \code{model_results}: List of individual model results (for diagnostics)
}
}
\description{
Computes a model-averaged future ATT estimate by running extrapolation with
each candidate model and combining results with exponential weights based on
cross-validation MSPE. Propagates EIFs through the weighted average for
valid uncertainty quantification.
}
\details{
\subsection{Algorithm}{
\enumerate{
\item Compute exponential weights: \code{w_m = exp(-MSPE_m / temp) / Z} where
\code{Z = sum(exp(-MSPE_m / temp))} ensures weights sum to 1.
\item For each model \code{m}, run \code{\link[=extrapolate_ATT]{extrapolate_ATT()}} to get \code{tau_m} and \code{phi_m}.
\item Compute weighted average: \code{tau_avg = sum(w_m * tau_m)}.
\item Propagate EIFs: \code{phi_avg = sum(w_m * phi_m)}.
}

The resulting \code{phi_avg} is the correct influence function for the
model-averaged estimator, enabling valid inference via the usual variance
formula: \code{Var(tau_avg) = mean(phi_avg^2)}.
}

\subsection{Temperature Parameter}{
\itemize{
\item \code{temperature = 1} (default): Standard exponential weighting
\item \code{temperature -> 0}: Winner-take-all (all weight on best model)
\item \code{temperature -> Inf}: Uniform weights (equal averaging)
}
}

\subsection{Reference}{

Implements the model averaging approach from Section 5.2 of the paper.
Exponential weights are a principled way to combine predictions based on
out-of-sample performance (analogous to stacking or Bayesian model averaging
with MSPE as pseudo-log-likelihood).
}
}
\examples{
# Create mock data (need sufficient time periods)
set.seed(789)
n <- 100
gt_data <- data.frame(
  g = rep(c(0, 1), each = 5),
  t = rep(1:5, 2),
  tau_hat = rnorm(10, 0.5, 0.1),
  k = rep(1:5, 2) - rep(c(0, 1), each = 5)
)

gt_obj <- list(
  data = gt_data,
  phi = lapply(1:10, function(i) rnorm(n, sd = 0.1)),
  times = 1:5,
  groups = c(0, 1),
  event_times = unique(gt_data$k),
  n = n
)
class(gt_obj) <- c("gt_object", "extrapolateATT")

# Run CV
models <- build_model_specs(c("linear", "quadratic"))
cv_result <- cv_extrapolate_ATT(gt_obj, models, horizons = 1:2,
                                 future_value = 6, compute_coverage = FALSE)

# Model-averaged extrapolation
avg_result <- average_models(cv_result, gt_obj, future_value = 6,
                              time_scale = "calendar", temperature = 1,
                              per_group = TRUE)

# Check weights (models with lower MSPE get higher weight)
print(avg_result$weights)

# Extract averaged estimate
print(avg_result$tau_g_future)

# Try different temperatures
# Winner-take-all (temperature near 0)
avg_concentrated <- average_models(cv_result, gt_obj, future_value = 6,
                                   time_scale = "calendar", temperature = 0.1,
                                   per_group = TRUE)
print(avg_concentrated$weights)

# Uniform (temperature large)
avg_uniform <- average_models(cv_result, gt_obj, future_value = 6,
                              time_scale = "calendar", temperature = 10,
                              per_group = TRUE)
print(avg_uniform$weights)

}
