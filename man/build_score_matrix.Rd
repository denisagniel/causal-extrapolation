% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_score_matrix.R
\name{build_score_matrix}
\alias{build_score_matrix}
\title{Assemble per-cell EIFs into a jointly-aligned score matrix}
\usage{
build_score_matrix(gt_object, ids = NULL, cluster = NULL, require_ids = TRUE)
}
\arguments{
\item{gt_object}{A \code{gt_object} carrying non-\code{NULL} \code{phi} (see \code{\link[=new_gt_object]{new_gt_object()}}).}

\item{ids}{Row keys. Either
\describe{
\item{\code{NULL} (default)}{use \code{gt_object$ids}; if that is also \code{NULL}, behavior is
governed by \code{require_ids}.}
\item{an atomic vector of length \eqn{n}}{a single key shared by every cell. This
documents the row identities but \emph{cannot} detect a per-cell permutation.}
\item{a list of \eqn{J} atomic vectors of length \eqn{n}}{per-cell keys. Each cell's
EIF is reordered onto the first cell's key, so a permuted cell is repaired. All
cells must carry the same \emph{set} of ids.}
}}

\item{cluster}{Optional cluster identifier, length \eqn{n}. If supplied, scores are
summed within cluster, yielding an \eqn{n_{\text{eff}} \times J} matrix with one row
per cluster (the standard cluster-robust influence-function aggregation). May be
unnamed (interpreted in row-key order) or named by unit id (looked up by name, so
ordering does not matter).}

\item{require_ids}{Logical, default \code{TRUE}. When no row key is available, error rather
than assume that all cells share a common row order. Set \code{FALSE} only when you know
the backend guarantees a consistent order; a warning is then emitted and
\code{seq_len(n)} is used as the key.}
}
\value{
A numeric matrix with \code{rownames} giving the row key (unit id, or cluster id when
\code{cluster} is supplied) and \code{colnames} giving the cell label \code{"g<g>_t<t>"}, in the row
order of \code{gt_object$data}. Attributes:
\describe{
\item{\code{cells}}{tibble of \code{g}, \code{t}, \code{k} and the column index \code{j}.}
\item{\code{clustered}}{logical, whether rows are clusters.}
\item{\code{n_eff}}{number of rows (independent units or clusters).}
\item{\code{J}}{number of cells (columns).}
}
Scaling follows the package convention: entries are \eqn{O_p(1)} per-unit scores with
\eqn{\mathrm{Var}(\widehat\theta_j) = \mathrm{Var}(\Phi_{\cdot j}) / n_{\text{eff}}},
matching \code{\link[=compute_variance]{compute_variance()}}. No rescaling is applied.
}
\description{
Reassembles the per-\eqn{(g,t)}-cell efficient influence functions stored on a
\code{gt_object} into a single \eqn{n \times J} score matrix whose rows are in guaranteed
correspondence across cells. Every first-stage backend in this package stores the EIFs
as an independent \emph{list} of length-\eqn{n} columns (\code{gt_object$phi}) — for example
\code{\link[=as_gt_object.MP]{as_gt_object.MP()}} splits \code{did::att_gt()}'s \code{x$inffunc} matrix column by column — which
discards the joint structure needed to estimate cross-cell covariances. This function
restores it.
}
\section{Why row keys are mandatory}{

Nothing in the \code{gt_object} contract guarantees that the \eqn{j}th EIF vector orders its
units the same way as the \eqn{j'}th. If they disagree, the assembled matrix has rows out
of correspondence, and \emph{every} off-diagonal covariance computed from it is attenuated
toward zero — silently, with no error and no warning, because the result is still a
perfectly well-formed matrix of the right dimensions. This function therefore refuses to
guess: with \code{require_ids = TRUE} (the default) it errors unless a reliable row key is
available. Supplying \code{ids} as a \emph{list} of per-cell id vectors additionally lets it
\emph{repair} a known misordering.
}

\examples{
\dontrun{
library(did)
data(mpdta)
att <- att_gt(yname = "lemp", gname = "first.treat", idname = "countyreal",
              tname = "year", data = mpdta)
gt <- as_gt_object(att)

# did does not return unit ids on the object, so key the rows explicitly.
Phi <- build_score_matrix(gt, ids = sort(unique(mpdta$countyreal)))
dim(Phi)
estimate_score_cov(Phi)$shrink_used
}

}
\seealso{
\code{\link[=estimate_score_cov]{estimate_score_cov()}} for the covariance of the returned matrix.
}
