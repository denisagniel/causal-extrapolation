% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_gt_object.R
\name{as_gt_object}
\alias{as_gt_object}
\alias{as_gt_object.gt_object}
\alias{as_gt_object.default}
\alias{as_gt_object.data.frame}
\title{Convert first-stage output to gt_object}
\usage{
as_gt_object(x, ...)

\method{as_gt_object}{gt_object}(x, ...)

\method{as_gt_object}{default}(x, ...)

\method{as_gt_object}{data.frame}(x, phi = NULL, se = NULL, n = NULL, ids = NULL, meta = NULL, ...)
}
\arguments{
\item{x}{Output from a first-stage estimator (e.g., \code{did::att_gt()},
\code{fixest::feols(..., sunab(...))}, or a data.frame with manual estimates).}

\item{...}{Additional arguments passed to methods.}

\item{phi}{List of EIF vectors (for data.frame method)}

\item{se}{Standard errors (for data.frame method)}

\item{n}{Sample size (for data.frame method)}

\item{ids}{Unit identifiers (for data.frame method)}

\item{meta}{Metadata list (for data.frame method)}
}
\value{
Object of class \code{gt_object}. See \code{?new_gt_object} for structure.
}
\description{
Generic S3 method to convert output from various first-stage DiD estimators
into the standardized \code{gt_object} format used by \code{extrapolate_ATT()}.
}
\details{
\subsection{Supported Methods}{

\strong{Built-in converters:}
\itemize{
\item \code{as_gt_object.AGGTEobj()} - Callaway & Sant'Anna (did package)
\item \code{as_gt_object.data.frame()} - Manual format (see below)
}

\strong{Manual format:}
If you have group-time ATT estimates from an unsupported method, you can:
\enumerate{
\item Format as data.frame with columns \code{g}, \code{t}, \code{tau_hat} (and optionally \code{k}, \code{se})
\item Call \code{as_gt_object(data, phi = ..., n = ...)}
\item Or use \code{new_gt_object()} directly for full control
}
}

\subsection{Extending}{

To add support for a new first-stage method:
\enumerate{
\item Implement \code{as_gt_object.yourclass()} method
\item Extract group-time ATTs and format as data.frame(g, t, tau_hat, k)
\item Extract EIF vectors if available (list of length-n vectors)
\item Call \code{new_gt_object(data, phi = phi, n = n, meta = list(source = "yourmethod"))}
}

See \code{vignette("first-stage-methods")} for examples.
}
}
\examples{
\dontrun{
# Example 1: From did::att_gt
library(did)
data(mpdta)
did_result <- att_gt(yname = "lemp", gname = "first.treat",
                     idname = "countyreal", tname = "year",
                     data = mpdta)
gt_obj <- as_gt_object(did_result)

# Example 2: Manual data.frame
data <- data.frame(
  g = c(2010, 2010, 2011),
  t = c(2012, 2013, 2012),
  tau_hat = c(0.5, 0.6, 0.4),
  se = c(0.1, 0.12, 0.09)
)
gt_obj <- as_gt_object(data, n = 100)
}

}
