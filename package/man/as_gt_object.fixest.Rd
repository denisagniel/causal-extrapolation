% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_fixest.R
\name{as_gt_object.fixest}
\alias{as_gt_object.fixest}
\title{Convert fixest sunab output to gt_object}
\usage{
\method{as_gt_object}{fixest}(x, extract_eif = TRUE, ...)
}
\arguments{
\item{x}{Result from \code{fixest::feols()} with \code{sunab()} specification.}

\item{extract_eif}{Attempt to extract influence functions? Default TRUE.
If unavailable, falls back to SE-only mode.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Object of class \code{gt_object}. See \code{?new_gt_object} for structure.
}
\description{
S3 method to convert output from \code{fixest::feols(..., sunab(...))} (Sun & Abraham)
into the standardized \code{gt_object} format. Extracts group-time ATTs from
cohort-time interaction coefficients.
}
\details{
\subsection{Sun & Abraham (2021) Method}{

The Sun & Abraham estimator uses interaction-weighted estimation to handle
staggered adoption with heterogeneous treatment effects. It specifies:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{fixest::feols(y ~ x + sunab(cohort_var, time_var) | unit + time, data = df)
}\if{html}{\out{</div>}}

This produces cohort-time interaction coefficients that represent group-time
ATTs. The converter extracts these coefficients and formats them for
extrapolation.
}

\subsection{What is extracted}{

\strong{From fixest output:}
\itemize{
\item Cohort-time interaction coefficients (group-time ATTs)
\item Standard errors from vcov
\item Sample size
\item Coefficient names parsed to extract (g, t) pairs
}

\strong{Influence functions:}
The converter attempts to extract influence functions via \code{influence.fixest()}
if available. If not available (depends on fixest version), falls back to
SE-only mode with a warning. Variance propagation will use delta method
approximation in SE-only mode.
}

\subsection{Coefficient name parsing}{

The converter handles multiple coefficient name formats from different fixest
versions:
\itemize{
\item \code{"cohort::2010:time::2012"} (standard format)
\item \code{"2010:2012"} (simplified format)
\item \code{"cohort::2010:rel_time::2"} (relative time format)
}

If parsing fails, an informative error is raised.
}

\subsection{Requirements}{
\itemize{
\item \code{x} must be from \code{fixest::feols()} with \code{sunab()} specification
\item The model must include cohort-time interaction terms
\item For EIF extraction: fixest version with \code{influence()} method (optional)
}
}
}
\examples{
\dontrun{
library(fixest)

# Simulate staggered adoption data
data <- sim_did_data(n = 1000, periods = 10)

# Estimate with Sun & Abraham
res <- feols(
  y ~ x + sunab(cohort, year) | unit + year,
  data = data
)

# Convert to gt_object
gt_obj <- as_gt_object(res)

# Check structure
head(gt_obj$data)
gt_obj$meta$source  # "fixest::sunab"

# Extrapolate
extrap <- extrapolate_ATT(
  gt_obj,
  h_fun = hg_linear,
  dh_fun = dh_linear,
  future_value = 5,
  time_scale = "event"
)
}

}
