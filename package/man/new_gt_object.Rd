% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_object.R
\name{new_gt_object}
\alias{new_gt_object}
\title{Create gt_object from raw components}
\usage{
new_gt_object(
  data,
  phi = NULL,
  n = NULL,
  se = NULL,
  ids = NULL,
  meta = NULL,
  ...
)
}
\arguments{
\item{data}{Data frame with columns \code{g} (group/cohort), \code{t} (time), \code{tau_hat}
(group-time ATT estimate). Optionally include \code{k} (event time = t - g);
if absent, it will be computed automatically.}

\item{phi}{List of EIF vectors (length n each), one per row of \code{data}. If NULL,
variance propagation will not be available (only point estimates).}

\item{n}{Sample size. Required if \code{phi} is provided; otherwise inferred.}

\item{se}{Optional: standard errors for each group-time ATT. Used for
approximate variance propagation via delta method when \code{phi} is NULL.}

\item{ids}{Optional: unit identifiers (length n). Useful for debugging.}

\item{meta}{Optional: list of metadata (e.g., source method, original object).}

\item{...}{Additional fields to include in the gt_object.}
}
\value{
Object of class \code{gt_object} with standardized structure:
\describe{
\item{data}{Tibble with columns g, t, k, tau_hat (and se if provided)}
\item{phi}{List of EIF vectors or NULL}
\item{times}{Sorted unique time points}
\item{groups}{Sorted unique groups}
\item{event_times}{Sorted unique event times (k = t - g)}
\item{n}{Sample size}
\item{ids}{Unit identifiers or NULL}
\item{meta}{Metadata list}
}
}
\description{
Construct a gt_object (group-time ATT container) directly from data components.
This is the low-level constructor for users with custom first-stage estimates
or methods not yet supported by built-in converters.
}
\details{
The gt_object format is the standardized internal representation used by
\code{extrapolate_ATT()} and downstream functions. It decouples the extrapolation
machinery from any specific first-stage method.

\strong{Uncertainty quantification:}
\itemize{
\item \strong{Full propagation (preferred):} Provide \code{phi} (EIF list). Enables exact
variance propagation through the extrapolation model via influence function
chain rule.
\item \strong{Approximate propagation:} Provide \code{se} without \code{phi}. Uses delta method
(assumes independence; may underestimate variance).
\item \strong{Point estimates only:} Omit both \code{phi} and \code{se}. Extrapolation will
produce point estimates without standard errors.
}
}
\examples{
\dontrun{
# Example 1: Manual format with EIF
data <- data.frame(
  g = c(2010, 2010, 2011),
  t = c(2012, 2013, 2012),
  tau_hat = c(0.5, 0.6, 0.4)
)
phi <- replicate(3, rnorm(100), simplify = FALSE)
gt_obj <- new_gt_object(data, phi = phi, n = 100)

# Example 2: Manual format with SE only
data$se <- c(0.1, 0.12, 0.09)
gt_obj <- new_gt_object(data, se = data$se, n = 100)

# Example 3: Point estimates only
gt_obj <- new_gt_object(data, n = 100)
}

}
