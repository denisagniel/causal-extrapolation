% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_did.R
\name{as_gt_object.AGGTEobj}
\alias{as_gt_object.AGGTEobj}
\title{Convert did::att_gt output to gt_object}
\usage{
\method{as_gt_object}{AGGTEobj}(x, extract_eif = TRUE, ...)
}
\arguments{
\item{x}{Result from \code{did::att_gt()} (class \code{AGGTEobj} from att_gt, not aggte).}

\item{extract_eif}{Extract influence functions? Default TRUE. Set to FALSE
for faster conversion if you only need point estimates.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Object of class \code{gt_object}. See \code{?new_gt_object} for structure.
}
\description{
S3 method to convert output from \code{did::att_gt()} (Callaway & Sant'Anna)
into the standardized \code{gt_object} format. Extracts group-time ATTs and
efficient influence functions (EIFs) for variance propagation.
}
\details{
\subsection{What is extracted}{

\strong{From att_gt output:}
\itemize{
\item Group-time ATT estimates (\code{att})
\item Group identifiers (\code{group})
\item Time points (\code{t})
\item Event times (computed as \code{k = t - group})
\item Standard errors (\code{se}) if available
}

\strong{Influence functions:}
The \code{did} package stores EIFs in \code{x$inffunc}, an n × J matrix where:
\itemize{
\item Rows correspond to units (n = sample size)
\item Columns correspond to group-time pairs (J = number of (g,t) cells)
\item Column j contains the EIF for the jth group-time ATT
}

These are converted to a list of length-n vectors for use in
\code{extrapolate_ATT()} and \code{compute_variance()}.
}

\subsection{Requirements}{
\itemize{
\item \code{x} must be from \code{did::att_gt()}, not \code{did::aggte()} (which aggregates
across group-time cells and loses the granular structure needed for
extrapolation).
\item If \code{extract_eif = TRUE}, \code{x$inffunc} must be present. This requires
\code{did} version >= 2.1.0 and the \code{bstrap = FALSE} option in \code{att_gt()}.
}
}
}
\examples{
\dontrun{
library(did)
data(mpdta)

# Estimate group-time ATTs
did_result <- att_gt(
  yname = "lemp",
  gname = "first.treat",
  idname = "countyreal",
  tname = "year",
  data = mpdta,
  bstrap = FALSE  # Needed for inffunc
)

# Convert to gt_object
gt_obj <- as_gt_object(did_result)

# Check structure
names(gt_obj)
head(gt_obj$data)
length(gt_obj$phi)  # Should equal nrow(gt_obj$data)
}

}
