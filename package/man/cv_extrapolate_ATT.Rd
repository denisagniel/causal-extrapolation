% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv_extrapolate_ATT.R
\name{cv_extrapolate_ATT}
\alias{cv_extrapolate_ATT}
\title{Time-Series Cross-Validation for Temporal Extrapolation Models}
\usage{
cv_extrapolate_ATT(
  gt_object,
  model_specs,
  horizons,
  future_value,
  time_scale = c("calendar", "event"),
  compute_coverage = FALSE,
  omega = NULL,
  level = 0.95,
  ...
)
}
\arguments{
\item{gt_object}{An object produced by \code{\link[=estimate_group_time_ATT]{estimate_group_time_ATT()}}.}

\item{model_specs}{A named list of model specifications. Each element must
contain: \code{h_fun}, \code{dh_fun}, and \code{name}. Use \code{\link[=build_model_specs]{build_model_specs()}} to
construct standard specifications.}

\item{horizons}{Integer vector of forecast horizons to test (e.g., \code{1:3}).
For each horizon \code{h}, the function holds out the last \code{h} periods as test
data and fits models on earlier periods.}

\item{future_value}{Numeric scalar: the actual future time to extrapolate to
(e.g., \code{p + 1}). This is used to determine the data range and is distinct
from the CV horizons.}

\item{time_scale}{One of \code{"calendar"} or \code{"event"}; determines whether
sequences are constructed over calendar times \code{t} or event times \code{k = t - g}.}

\item{compute_coverage}{Logical; if \code{TRUE}, compute 95\% confidence interval
coverage rates for each model (requires EIF propagation, computationally
expensive). Default: \code{FALSE}.}

\item{omega}{Optional numeric vector of group weights for aggregation. If
provided, computes aggregated predictions across groups. If \code{NULL}, uses
equal weights.}

\item{level}{Confidence level for coverage computation (default 0.95).
Only used if \code{compute_coverage = TRUE}.}

\item{...}{Additional arguments passed to \code{h_fun} and \code{dh_fun}.}
}
\value{
An S3 object of class \code{cv_extrapolate} with CV results and diagnostics.
}
\description{
Implements the time-series CV procedure from Section 5.2 to evaluate candidate
temporal models for extrapolation. Tests each model by holding out late periods,
fitting on earlier data, and computing mean squared prediction error (MSPE) on
held-out future observations.
}
\details{
\subsection{Algorithm}{

For each horizon \code{h} in \code{horizons}:
\enumerate{
\item Split data: training window is \code{t <= max(t) - h}, test window is
\code{t > max(t) - h}.
\item For each model in \code{model_specs}:
\itemize{
\item For each (group, test_time) pair in test window:
\itemize{
\item Fit model on training data for that group
\item Extrapolate to test_time
\item Compare prediction to observed ATT
}
\item Compute MSPE: \verb{(1 / |G| * h) * sum((predicted - observed)^2)}
}
\item Average MSPE across horizons: \code{MSPE_m = mean(MSPE_m(h))}
\item Select best model: \code{argmin(MSPE_m)}
}
}

\subsection{Return Structure}{

An S3 object of class \code{cv_extrapolate} containing:
\itemize{
\item \code{results}: Tibble with columns \code{model}, \code{horizon}, \code{mspe}, \code{coverage}
\item \code{best_model}: Name of model with lowest average MSPE
\item \code{avg_mspe}: Average MSPE for each model across horizons
\item \code{predictions}: Detailed predictions for each (model, horizon, group, time)
\item \code{model_specs}: The input model specifications
\item \code{horizons}: The input horizons
\item \code{compute_coverage}: Whether coverage was computed
}
}
}
\section{Reference}{

This implements the time-series cross-validation procedure from Section 5.2
of the paper. See \code{\link[=select_best_model]{select_best_model()}} for model selection and
\code{\link[=average_models]{average_models()}} for model averaging with exponential weights.
}

\examples{
# Create example data with quadratic trend
set.seed(123)
n <- 100
groups <- c(0, 1)
times <- 1:6

data_list <- list()
phi_list <- list()
idx <- 1

for (g in groups) {
  for (t in times) {
    # Quadratic DGP
    tau_true <- 0.1 + 0.05 * t + 0.02 * t^2
    data_list[[idx]] <- data.frame(g = g, t = t,
                                    tau_hat = tau_true + rnorm(1, sd = 0.01),
                                    k = t - g)
    phi_list[[idx]] <- rnorm(n, sd = 0.1)
    idx <- idx + 1
  }
}

gt_obj <- list(
  data = do.call(rbind, data_list),
  phi = phi_list,
  times = 1:6,
  groups = groups,
  event_times = unique(do.call(rbind, data_list)$k),
  n = n
)
class(gt_obj) <- c("gt_object", "extrapolateATT")

# Build model specifications
models <- build_model_specs(c("linear", "quadratic"))

# Run time-series CV
cv_result <- cv_extrapolate_ATT(
  gt_obj,
  model_specs = models,
  horizons = 1:2,
  future_value = 7,
  time_scale = "calendar",
  compute_coverage = FALSE
)

# View results
print(cv_result)
summary(cv_result)
plot(cv_result)

# Quadratic should win (data is quadratic)
print(cv_result$best_model)

# Use selected model for extrapolation
best_spec <- models[[cv_result$best_model]]
result <- extrapolate_ATT(
  gt_obj,
  h_fun = best_spec$h_fun,
  dh_fun = best_spec$dh_fun,
  future_value = 7,
  time_scale = "calendar"
)

}
