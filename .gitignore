.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store

# Agent memory store (machine-specific; directory name encodes an absolute path)
.claude/projects/

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.fdb_latexmk
*.fls
*.out
*.synctex.gz
*.xdv

# Compiled manuscript PDFs (regenerated by /compile-latex; source .tex is tracked).
# Reference PDFs (refs/) and figure PDFs are tracked separately and not ignored.
inst/paper/main.pdf
inst/paper/main-lean.pdf

# Redundant raw archive (CSVs extracted alongside)
application/data/rstudio-export.zip
tests/testthat/Rplots.pdf
