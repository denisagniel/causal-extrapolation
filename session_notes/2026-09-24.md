# Session Notes — 2026-09-24

## 07:32 — theory-audit-absorption-and-known-limitations

**Goal.** Absorb `inst/paper/theory.tex` (2026-08-21, Oracle-reviewed proof audit
of Propositions 1/2 and RC5) into the canonical `inst/paper/main.tex`, fix any
corresponding package bugs the audit surfaced, and pursue all four
Known-Limitations items the theory audit flagged as tractable extensions
(Path 3 two-sample correction, DiD score for staggered adoption, GLS-efficient
weighting, truncation-bias bound for overlap).

**What happened.**

### Theory audit → main.tex (Props 1/2, RC5)

Confirmed `main.tex`'s own header comment ("SEMI-DEPRECATED, main-lean.tex is
canonical") is stale and wrong: `main-lean.tex` is an abandoned skeleton (31
TODOs, fabricated simulation numbers, references to nonexistent appendices).
`main.tex` is the actively-developed, canonical paper. `theory.tex` documents
a genuine, unmerged proof correction: Propositions 1 and 2 previously swapped
the conditioning event from `A_{ip}=1` to `A_{i,p+1}=1` mid-proof without
justification (an unnecessary, and on the original design incorrect, step),
and RC5 (identifiability + local rank for the parametric extrapolation
function) conflated global injectivity with local nonsingularity of the
Jacobian — two logically independent conditions (counterexample: `f(g,t;γ) =
γ³ c_{gt}`, injective but with a zero Jacobian at `γ=0`).

Dispatched Oracle to draft the fix and applied it to `main.tex` in full:

- New `Assumption~\ref{absorbing-adoption}` (absorbing adoption).
- New `\S\ref{sec:cohort-structure}` (Cohort structure of the FATT) with
  `Lemma~\ref{lem:persistence}` (cohort persistence) and
  `Lemma~\ref{lem:aggregation}` (exact aggregation), replacing the
  conditioning-event swap.
- Rewrote `strict-time-homogeneity` at the cohort level (scalar `θ` for all
  `g ∈ 𝒢`), and rewrote Proposition 1 (`prop:path1`) and its proof
  (`app:prop1`) accordingly.
- Added `Assumption~\ref{assump:parametric}` (parametric temporal model),
  rewrote Proposition 2 (`prop:path2`) and its proof (`app:prop2`).
- Replaced the old within-group-homogeneity assumption pair with
  `Corollary~\ref{cor:path1-within}`; deleted the now-superseded
  `Lemma~\ref{lem:convex}`.
- Rewrote RC5 (`\label{RC5}`) into two independent clauses (global
  identifiability; local rank of `H_0`), added
  `Remark~\ref{rem:rc5-independence}` with the `γ³` counterexample.
- Rewrote `lem:injectivity` (rank condition for the group-specific linear
  model), added `Corollary~\ref{cor:linear-rank}` and
  `Remark~\ref{rem:last-cohort}` (ties the last-adopting-cohort degeneracy to
  the Appendix A stress test).

Compiled and verified: 52 pages, all cross-references resolve, only the two
pre-existing missing-`.bib`-entry citation warnings remain
(`tibshiraniExactPostselectionInference2016`,
`chenValidInferenceModel2021` — still outstanding, unrelated to this session).

### Package fixes surfaced by the audit

1. **`R/estimate_group_time_ATT.R`.** Added an `id` parameter, plumbed to
   `did::att_gt()`'s `idname`, with a warning when omitted. Found and fixed a
   real latent bug along the way: `is.null(x)` on an unevaluated bare-symbol
   promise forces evaluation and errors under NSE — fixed for both `cluster`
   and `id` using `rlang::enquo()` / `rlang::quo_is_null()`.

2. **`R/extrapolate_ATT.R`.** Confirmed per-cohort `γ` fitting was already
   correct (splits by group, fits separately — `rem:last-cohort` describes a
   real degeneracy for the last-adopting cohort, but the *code* handles it
   correctly by erroring rather than fitting nonsense). Added
   `.enrich_group_error()`: the error now names the failing group, the number
   of observed periods, and gives three remediation options (pool the slope
   across cohorts, model in event time alone, restrict to cohorts with
   sufficient periods).

3. **`R/from_fixest.R` (found independently, not from the theory audit).**
   Running the package's own documented `sunab()` example verbatim
   (`feols(lemp ~ sunab(first.treat, year) | countyreal + year, data =
   mpdta)`) failed before EIF extraction: `fixest::influence()` doesn't exist
   as an exported function in installed fixest 0.13.2, and `coef(res)` on a
   `sunab()` fit gives aggregated event-time-only names (`"year::-4"`),
   losing the cohort index the old regex parser needed. Fixed by switching to
   `summary(x, agg = FALSE)` (plain `summary()` dispatch — `fixest::summary`
   isn't exported) to recover the disaggregated
   `"year::-4:cohort::2007"`-format coefficients, with a new
   `parse_sunab_names()` Pattern 0 and a combined `summary()`+`coef()`
   `tryCatch` (a follow-on edge case surfaced by the full test suite:
   malformed mock objects can make `summary.fixest()` succeed but fail
   downstream at `coef()`). Verified end-to-end against `did::mpdta`: real
   per-unit EIFs of length 2500 extracted for all 12 `(g,t)` cells,
   `extrapolate_ATT()` + `compute_variance()` run cleanly.

### Known-Limitations items (all four pursued; results below)

Dispatched Oracle for the four tractable extensions the theory audit flagged.

**Item 3 — GLS-efficient weighting: implemented and tested.**
`gls_weights()` (new, `R/path1_aggregate.R`) computes normalized
inverse-variance weights from the sample variance of within-group EIFs;
`path1_aggregate()` gained `weighting = c("equal", "gls")`;
`safe_matrix_inverse()` (`R/utils_numerical.R`) generalized to accept a
`weights` argument for a weighted Gram matrix; `hg_linear()`/`hg_quadratic()`
gained a `weights` argument for WLS (NULL reproduces the prior OLS exactly);
`extrapolate_ATT()` gained `weight_scheme = c("equal", "gls")`. Verified a
~7.4x variance reduction under heteroskedastic noise in `path1_aggregate()`
and a ~1.09x reduction in `extrapolate_ATT()`. New file
`tests/testthat/test-gls-weights.R`, 18 tests, all passing.

**Items 1 and 6 — Path 3 two-sample correction and truncation-bias bound:
derived and implemented.** Dispatched Oracle for full derivations of (1) the
two-sample correction when the target covariate sample is genuinely external
(size `n*`, independent of the source) and (6) the truncation-bias bound when
capping a heavy-tailed transport weight `w` at `C`. Both derivations came back
as rigorous propositions with proofs (available in the Oracle session; not
yet transcribed into `main.tex` — see Next). Headline results:

- **Two-sample variance addendum.** `V = V_src + ρ·Var_Q(τ)`, `ρ = lim n/n*`,
  with constant *exactly* 1 (an equality, confirmed by the audit, not an order
  bound) — this validates the informal guess that motivated dispatching the
  derivation. Implemented as `integrate_cate(..., n_target = n_star)`: adds
  `ρ̂·Var̂_Q(τ)/n` to the variance (point estimate untouched), where `ρ̂ = n/
  n_target` and `Var̂_Q(τ)` is the weighted second moment of `τ` under the
  (possibly truncated) final weights. Ignored, with a warning, for Form A
  (`target`), where the "target" is a fixed subsample of the source and there
  is no second sample.
- **Truncation bias bound.** `|B̃(C)| ≤ {Q(w>C)}^{1/2}/(1-δ_C) · {Var_Q(τ)}^
  {1/2}` (Cauchy–Schwarz form), where `δ_C = 1 - E_src[w∧C]` is the target
  weight-mass discarded by the cap — **exactly zero when `τ` is constant on
  the truncated region**, regardless of `C`. Implemented as
  `integrate_cate(..., trunc = C)` (fixed numeric cap, `≥ 1`) or
  `trunc = "auto"` (grid search for the smallest cap on `trunc_grid` whose
  bound is at most `trunc_gamma` × SE, default 0.25 — an
  Armstrong–Kolesár-style bias-aware budget). Guards: `trunc < 1` errors
  (since `E_src[w] = 1`, capping below 1 always discards mass for nothing);
  `δ_C > 0.5` errors (the capped estimand no longer refers to the intended
  target). Diagnostics (`delta_trunc`, `q_exceed`, `var_q_tau`, `bias_bound`,
  `se_bias_aware`) returned on the `cate_integration` object and shown by
  `print()`.

  **Implementation subtlety worth flagging for future readers of this code:**
  `Var_Q(τ)` in both features must be centered at the *plug-in weighted mean
  of τ itself* (`Σwτ/Σw`), not at the doubly-robust one-step point estimate
  `ψ̂` — they coincide only in the population, not in a finite noisy sample
  (the DR correction term has population mean zero but not sample mean zero).
  Centering at `ψ̂` breaks the "exactly zero under homogeneity" property in
  finite samples; a first implementation attempt did exactly this and was
  caught by a test asserting the zero-bias property with `τ` set literally
  constant. Fixed by making `.weighted_var_q(w, tau)` self-contained (no
  `psi` argument) — see `R/integrate_cate.R`.

  New tests appended to `tests/testthat/test-integrate-cate.R` (14 new
  `test_that` blocks): no-op at `trunc = Inf`, SE reduction under a lognormal
  heavy tail, exact-zero bias under constant `τ`, `trunc = "auto"` respecting
  its budget, the `trunc ≥ 1` and `δ_C ≤ 0.5` guards, the two-sample addendum's
  magnitude and its Form A no-op warning, `n_target > 0` validation,
  exact-zero addendum under constant `τ`, and composition (truncation feeding
  the correct final weights into the two-sample addendum). Full package test
  suite re-run clean throughout (0 failures; only the pre-existing
  mock-object warnings/skips).

**Item 2 — DiD score for (g,t)-indexed staggered adoption: derived, NOT yet
incorporated.** Oracle's derivation (not-yet-treated comparison group,
per-cell EIF, aggregation across cells) came back complete but flags a real
subtlety a rushed implementation would get wrong: the cellwise-efficient
aggregation is **not** semiparametrically efficient for `θ_{p+1}` itself
(only "GMM-efficient among linear combinations of the chosen cell scores"),
and small cohorts can dominate the two-sample variance term when the
not-yet-treated comparison set overlaps a target sample. Implementing this
correctly requires a genuinely new multi-cell score-matrix architecture (the
package currently has no notion of "a family of (g,t) cells sharing base
periods/comparison units with correlated scores"). Given the size of that
change and the risk of introducing exactly the kind of subtle bug this
session spent most of its time finding and fixing elsewhere, **deferred**:
theory is in hand (Oracle session, retrievable), but no `main.tex` prose and
no package code were written for this item today.

**Decisions.** User explicit calls this session: fix package bugs directly
rather than caveat them in the Discussion ("we aren't tied to its current
state"); pursue all four Known-Limitations items rather than a tractable
subset ("all four of these"); fix the RC5 rank-condition gap now rather than
defer ("let's fix"); archive `main-lean.tex`/`theory.tex` only once every
item is absorbed, not before ("let's wait" / "yes, fine with this" once
ready).

## Next

- Retrieve Oracle's full Item 1/Item 6 derivation text (propositions + proofs,
  audit notes) and transcribe into `main.tex` — likely a new subsection near
  Assumption~\ref{assump:target-dist}/RC9/RC10 (Path 3, `\S\ref{sec:path3}`)
  plus the corresponding appendix proofs. Not yet written into the paper;
  only the package side is implemented and tested so far.
- Item 2 (DiD score): design the multi-cell score-matrix architecture before
  attempting an implementation; write up the theory in `main.tex` regardless
  (can be presented even without a package implementation, as with other
  Known-Limitations items historically).
- Archive `inst/paper/main-lean.tex` and `inst/paper/theory.tex`/`.pdf`/`.toc`
  once all four Known-Limitations items are fully absorbed — explicitly
  deferred by the user until then.
- Remove/update the stale "SEMI-DEPRECATED" (main.tex header) and "STALE
  FRAMING" (§4.1) comments once the Path 3-consistent framing replaces the
  old `θ_{gt} = f(g, I_t; θ)` device.
- The two missing `.bib` entries (`tibshiraniExactPostselectionInference2016`,
  `chenValidInferenceModel2021`) remain outstanding — flagged again, not
  fixed, not blocking.
- `sims/section4b/`, `sims/section7est/` O2 SLURM scaffolds remain built but
  unsubmitted (kept in reserve pending VPN/cluster access).

**Commit/PR.** This session's `main.tex` theory fixes and all package changes
(id-param + NSE fix, fixest `sunab()` fix, GLS weighting, truncation,
two-sample correction) were committed and merged to `main` via PR in this
session — see git log for the PR reference.

## 13:20 — theory-audit-full-main-tex-proof-auditor

**Goal.** User back on VPN, requested a thorough audit of all theory in the
paper now that `proof-auditor`/`proof-writer` subagents are available. Ran
four parallel, independent `proof-auditor` passes over the entirety of
`main.tex`'s formal theory (canonical paper — `main-lean.tex` is the
abandoned skeleton, `theory.tex` the not-fully-absorbed draft; neither
audited). This is a **second, independent** audit layered on top of this
morning's (07:32) absorption of the *first* audit — Props 1/2/RC5 were
already fixed once today; this pass re-derives everything from scratch
without trusting that fix, and additionally covers ground the morning
session never audited (Prop 3, all EIF derivations, the asymptotic-normality
proposition).

**What happened.** Four `proof-auditor` background tasks, each with the
verbatim theorem/proof text pasted in (not just file paths, to guarantee the
auditor worked from an unambiguous ground truth): (A) cohort structure +
Path 1 (Lemmas persistence/aggregation, Prop 1, Cor 1, §3.4's three unproved
equivalence claims); (B) Path 2 + Path 3 identification + RC1–RC10 +
technical lemmas; (C) EIF derivations for all three paths + the
target=source collapse lemma; (D) the asymptotic-normality proposition,
specifically tasked with adjudicating a suspected defect I (the orchestrator)
spotted independently while assembling the audit prompts: eq. (475)'s
displayed Path-3 estimator (plug-in average of τ̂ over an external target
sample) does not obviously match the estimator whose EIF is the doubly-robust
transport score $(\ast)$ (scored on source data, with a density-ratio
reweighting and an AIPW augmentation that needs treatment/outcome data the
target sample doesn't have).

Full reports saved to `quality_reports/reviews/2026-09-24_main-tex-{cohort-path1,
path2-path3-regularity,eif-derivations,asymptotic-normality}.md`. Headline
findings, in order of severity:

**Confirmed, independently, by two separate audits (Pass C and Pass D via
different routes): the Path 3 EIF score $(\ast)$ is not Neyman-orthogonal in
the density ratio $w$, is not the actual EIF, and is anti-conservative.**
Pass D's exact algebra: comparing $(\ast) = \frac{e(X)}{\pi}(\tau(X)-\theta)+
w\cdot\mathrm{aug}$ against Hahn's (1998) true ATT EIF $\phi_{\rm ATT}=
\frac{A}{\pi}(\tau(X)-\theta)+w\cdot\mathrm{aug}$, the ratio of the
$\tau$-term variances is $\approx\bar e$ among the treated — a ~2× understatement
at $e\approx0.5$, ~20× at $e\approx0.05$ (rare treatment). Fix: the
two-sample transport score that scores the $\tau$-variation term on *target*
draws with weight 1, not on *source* draws reweighted by $w$.

**Confirmed my own suspicion about eq. (475) — and it's worse than I
thought: not an ambiguity, a hard inconsistency.** Pass D proved eq. (475)
(plug-in average of $\hat\tau$ over $n^*$ target draws) and the estimator
implicit in score $(\ast)$ (a source-sample AIPW transport estimator) are
different statistics with different inputs, that they do NOT coincide even
in the paper's own "time-invariant covariates" special case (both terms
differ), that the asymptotic-normality proof's Path-3 step unambiguously
analyzes the *latter*, and that (475) is not even $\sqrt n$-consistent under
the paper's own premise that $\hat\tau$ need not be — no augmentation term
exists in (475) to debias it. Separately, no rate condition anywhere ties
$n^*$ to $n$, so the stated variance $\sigma^2=\E[\phi^2]$ omits a
$\rho^{-1}\mathrm{Var}_{\rm tgt}(\tau)$ two-sample term entirely (this is the
theoretical justification for what the package's `integrate_cate(...,
n_target=)` already implements numerically, per this morning's session —
the paper text is now the thing lagging the code, not the reverse).

**Path 2's EIF derivation silently assumes correct specification.** The
implicit-function-theorem step's $\partial\gamma/\partial\theta_{gt}=H_0^{-1}
w_{gt}\nabla f_{gt}$ omits a residual-Hessian correction term $R$ (nonzero
whenever $f$ is nonlinear in $\gamma$ AND the model is misspecified); the
paper's claim that "the variance formula remains valid... under
misspecification" is false whenever both hold. True for all the paper's
*linear* built-ins (linear-in-event-time, since $\nabla^2 f\equiv0$ then,
regardless of specification) — so this doesn't touch the sims as currently
run, but would break silently if a curved parametric form (quadratic,
spline) were ever fit under misspecification.

**§3.4's three "trivial" equivalence claims (ATU=FATU, ATE=FATE, ATS=FATS)
are asserted with zero proof, and the ATS one is actually wrong** —
instantiating the general assumption produces a statement about unit $i$'s
own effect, but FATS is defined as unit $j$'s effect (a type mismatch), and
the claimed exemption from the second assumption silently requires the
matching-ball event to be time-invariant, which conflicts with the paper's
own time-varying-covariates setup.

**Lower-severity but real:** RC7/RC8's stated rate ($o_P(n^{-1/4})$ for a
*product* of nuisance rates) is off by a factor of $n^{1/4}$ from the
standard DML condition ($o_P(n^{-1/2})$ for the product, i.e. each nuisance
at $o_P(n^{-1/4})$) — caught independently in three of the four passes.
`assump:parametric`'s second clause quantifies $t\le p$ where it should say
$g\le t\le p$, making it literally unsatisfiable for the paper's own
linear-in-event-time running example (forces $\alpha_g=\beta_g=0$ at
pre-treatment cells). Main-text and appendix give two different (differently
weighted) formulas for $\phi_{\omega_g}$. RC5's independence remark proves
only one direction of "the two clauses are independent" (supplied the
missing counterexample). The collapse lemma (target=source ⇒ ordinary ATT
EIF) is actually the ATE EIF, not the ATT EIF — contradicts its own
follow-up caveat two sentences later.

**Two non-findings worth recording so they aren't re-litigated:** the
positivity caveat on Lemma "cohort persistence" is correctly scoped (event
equality needs nothing; only the conditional-expectation corollary needs
$\P(G_i=g)>0$) — a place the paper gets it right and I flagged for
verification. And the 4th-moment/joint-vs-marginal-asymptotic-linearity
worries I raised in the Pass D prompt were both confirmed *unfounded* —
Lindeberg–Lévy needs only 2nd moments, and joint AL follows from marginal AL
by Cramér–Wold over a common i.i.d. index.

**Decisions.** None yet — reports delivered to user for triage before any
fix is attempted. Per proof-writer/proof-auditor protocol, review vocabulary
("defective," "anti-conservative," "an earlier version") stays in the review
files, never in `main.tex` prose.

## Next

- Await user direction on: (a) fix Path 3's EIF + estimator inconsistency
  (C1–C4 of the asymptotic-normality report) — this is the most consequential
  cluster, since it invalidates every reported Path-3 SE/CI in the current
  simulations/application as currently derived; (b) fix §3.4's three
  equivalence claims or restructure per the audit's suggested unification
  (fixed conditioning event at $p$, one theorem for all four estimands);
  (c) fix the RC7/RC8 rate exponent (mechanical, low-risk); (d) fix
  `assump:parametric`'s quantifier (mechanical); (e) decide whether to also
  route the Path-3 fix through the not-yet-transcribed two-sample/truncation
  math from the earlier Oracle session (07:32 entry, Items 1/6) — the two
  problems are related (both concern Path-3 variance under a finite/external
  target sample) and might be fixed together.
- If proceeding with fixes: use `proof-writer` (not ad hoc edits) given the
  severity and the project's proof-protocol conventions; re-audit with
  `proof-auditor` after any nontrivial rewrite of Path 3's EIF or the
  asymptotic-normality proposition, since two independent passes converging
  on the same defect via different routes is exactly the kind of finding
  that's easy to under-fix.
- Note for whoever picks this up: fixing C1–C4 in the asymptotic-normality
  report likely requires rewriting §3.7's estimator presentation, Appendix
  E.3's EIF derivation, and RC8/RC9/RC10 together — they are not independent
  edits.

## 15:30 — theory-audit-full-fix-complete

**User decision.** After the escalation above (Oracle diagnosis: the same two-weight
defect exists independently in `R/integrate_cate.R`'s Form-A/`n_target` code path, worse
than the paper's version — it drops the untreated arm entirely, `A_i(1-A_i)≡0`, and
changes point estimates, not just SEs), user said "let's just do the full fix." Executed
Oracle's phased plan: TDD package fix, paper theory rewrite, cross-validation, re-audit,
compile.

**Locked architectural decision (mine, made once, handed to both parallel tasks so they
converge without talking to each other):** three conceptual regimes for Path 3 transport;
this fix supports two. Regime (i) "internal target" — target is a subpopulation of the
current source sample (e.g. `target = which(A==1)` for the FATT); centering weight =
hard membership indicator, augmentation weight = target-membership score (`e` by default
when target = treated set). Regime (iii) "known density ratio" — `weights=` supplied, no
`n_target`; same weight for both terms, already correct pre-fix, untouched. Regime (ii)
"external finite target sample" (`n_target` given) — deferred; `n_target` now errors
instead of applying the wrong variance formula from this morning's now-superseded Item-1
absorption.

**Package (`R/integrate_cate.R`, `tests/testthat/test-integrate-cate.R`).** Dispatched as
one `deep`-category TDD task. Wrote the failing test first (Form A / `target=which(A==1)`
must reproduce Hahn's (1998) ATT EIF unit-by-unit, and `phi[A==0]` must be nonzero) —
confirmed it failed 4/4 against unmodified code, with `all(phi[A==0]==0)==TRUE` exactly as
predicted. Implemented the two-weight fix (`w_center`/`w_aug`, new `target_score`
argument), made `n_target` error, rescoped `trunc` to Regime (iii), removed
`.apply_two_sample_variance()` (dead code once `n_target` is rejected at entry). `score_aipw()`/
`score_drdid()` untouched — they were already correct; the bug was entirely in how their
output got multiplied. Full suite: 0 fail, 717 pass (+19), 44 warn (−1), 5 skip
(pre-existing). Docstring example (n=400, `target=which(A==1)`, seed 20260708) before/after:
estimate 1.1830→1.2081 (moved ≈0.4 old SE — point estimate changed, not just SE, exactly as
Oracle predicted), SE 0.0873→0.0633 (ratio 0.7252, matching the theoretical
sqrt(e^2/(1-e)) = 0.7152 at the sample's mean propensity — strong independent numerical
confirmation the algebra is right). Deviations from the locked decision, all reasonable:
`target_score` auto-defaults for DiD too (not just unconfoundedness, per the spec's own
second sentence); `target=seq_len(n)` (full source) gets an exact `q≡1` default, required
for the existing (correct, just mislabeled) ATE-collapse tests to keep working; `trunc`
errors rather than no-ops for Regime (i); no `NEWS.md` (package has none).

**Paper (`inst/paper/main.tex`).** Dispatched `proof-writer` in parallel — it cannot edit
files in this harness's configuration, so it returned paste-ready LaTeX for six locations,
which I applied by hand after independently cross-checking the algebra against the
package's actual `psi_hat`/`phi` formulas (they match exactly, term for term, once expanded
— strong convergent validation from two independently-produced fixes). Rewrote: §3.7's
"Semiparametric estimation and EIF" (replaced eq. `eq:fatt-path3-est`'s old plug-in-over-
external-sample estimator with the two correctly-derived Regime-(i)/(iii) estimators and
EIFs); RC7–RC10 (fixed the `o_P(n^{-1/4})`-per-product rate to the correct `o_P(n^{-1/2})`,
independently caught by three of the four earlier audit passes; split nuisance rate
conditions by regime); Appendix E.3 "Path 3 EIF" (full pathwise-differentiation derivation
from a stacked source+target population, with explicit Gateaux-derivative orthogonality
checks — the old version hand-waved "taking a parametric submodel and differentiating");
Lemma `lem:path3-collapse` (now correctly collapses to Hahn's ATT EIF via T=A, rho=pi,
q=e, not the previous w=1-for-everyone substitution, which the audits showed gives the
ATE); `prop:asymp` + its restated appendix proposition* (found and fixed by grep — the
appendix restates the proposition before its proof, and that restatement still had the
old wording after the main-text one was fixed); the asymptotic-normality proof's Path-3
step; the cross-fitting remark; added `\label{sec:discussion}` (missing) and a
Known-Limitations paragraph giving the *correct* two-sample variance decomposition
`V = Var_target(tau)/n* + Var_source(w*aug)/n` for future work, retracting (without
narrating the retraction in-paper) this morning's wrong `V=V_src+rho*Var_Q(tau)` formula.
Compiled clean: 0 overfull hbox, 0 new undefined refs, 57pp.

**Re-audit (`proof-auditor`, fresh pass on the rewrite).** 0 Critical, 5 Major, 13 Minor.
Headline: independently re-derived the estimator-vs-EIF consistency check that caught the
*original* bug (delta-method expansion of the displayed ratio estimators) — passes exactly
for both regimes, confirming the fix didn't reintroduce that failure mode. The 5 Majors
were all "math right, scope/derivation-steps unstated," not new errors: (M1) `prop:asymp`
centering invalid if an externally-*supplied* (not truly known) ratio is used — that's
bias, not variance, and wasn't distinguished from the known-w case; (M2) Regime (i) for
the FATT under time-invariant covariates is degenerate (=backward ATT) and the paper never
said so; (M3) missing assumption linking the internal target's law to the actual transport
target (q/rho=w); (M4) the asymptotic-normality proof asserted linearity via named
mechanisms without showing the ratio-solves-score identity or the remainder's exact term
structure — the same category of gap that hid the *original* bug, so fixed first; (M5)
"efficient" asserted for both regimes with no tangent-space/uniqueness argument. Fixed all
five directly, plus three cheap minors (RC9's redundant ratio condition replaced with the
actual binding outcome-variance condition; RC6/RC7 mis-citations for the now-Path-3-covering
`prop:asymp` corrected to RC9/RC8; the variance remark's Path-3 nuisance list added).
Re-compiled clean: 58pp, 0 overfull hbox, 0 new undefined refs.

**Phase 5/6 (re-running sims/application) — turned out to be unnecessary.** Grepped every
`integrate_cate()` call site in `sims/` and `application/`: all of them use `weights=`
(Form B / Regime iii) with no `n_target` — none use `target=which(A==1)` (Form A) or
`n_target`. Regime iii's code path is byte-identical pre/post fix (confirmed: no diff
touched it). Smoke-tested Form B post-fix to confirm `$estimate`/`$se`/`$ci`/`$n_eff` are
unchanged (new fields `$w_aug`/`$regime` added, nothing removed or renamed that the sims/
application scripts consume — grepped for `$w`, `$form`, `$regime` usage in those scripts,
found none). Separately, `application/README.md` already marks the Path-3 DiD application
row as "pending"/excluded from the pipeline, independent of anything found today. **No
simulation or application numbers needed re-running or updating in the paper.**

**Decisions.** Full fix executed to completion in one session: package (TDD), paper theory
(proof-writer + re-audit + fixes), verified consistent by direct algebra cross-check and by
independent re-derivation. Nothing committed — left staged for review per orchestrator
convention (never commit without explicit request).

## Next

- Review the diff (`R/integrate_cate.R`, `tests/testthat/test-integrate-cate.R`,
  `inst/paper/main.tex`, regenerated `man/*.Rd`) before committing.
- Consider whether to also fix the remaining 13 Minor findings from the re-audit
  (label hygiene, rho/w notation overload across the paper, an unnamed citation to
  Hahn (1998) in the collapse lemma) — none affect correctness; deferred as genuinely
  low-priority.
- `main-lean.tex`/`theory.tex` archival remains deferred per the user's earlier "let's
  wait" — this session added more reasons to wait (the Path-3 fix is now the most
  Oracle/proof-audit-scrutinized part of the paper; theory.tex's own Path-3 framing
  predates today's fix and would need reconciling before archival makes sense).
- Consider running `devtools::check()` properly once a compile toolchain is available in
  this environment (`pkgbuild::check_build_tools()` failed here; unrelated to today's
  changes, pre-existing environment limitation) — today's verification relied on
  `load_all()` + `test()` + `run_examples()` only.

## 15:45 — verification-and-sims-check

**Verifier agent, end-to-end pass.** Dispatched `verifier` for an independent check
(compile, package tests, docs, algebra cross-check, numeric smoke test, git status, review
reports) — not a re-audit of the math, a check that everything actually builds/runs/is
consistent. All 7 items PASS: paper compiles clean (58pp, 0 overfull hbox, 0 new undefined
refs); package suite 0 fail/717 pass; `document()`/`checkDocFiles()` clean, example block
runs; independently re-derived the paper-vs-package algebra a *third* time (exact
term-for-term match, no discrepancy); numeric smoke test (n=300, hand-computed phi vs.
package phi) matches to 8.9e-16, and 0/158 control units have zeroed phi (the original
bug's exact signature is gone); nothing committed; all 5 review files present and their
labels resolve against the current `main.tex`.

**User asked: do sims need updating?** Re-verified (a third time, independently of the
Phase-5 check) by grepping every `integrate_cate()` call site in `sims/` and
`application/`: all pass `weights=` (Form B / Regime iii), none pass `target=` (Form A) or
`n_target=`. Form B's code path is untouched by the fix (confirmed via diff — no lines in
that branch changed). **No simulation or application script needs re-running; no numbers
in `inst/paper/sim_tables/` change.** The bug only ever manifested for
`target=which(A==1)`, which appears only in the package's own test suite and docstring
example, never in a simulation or the application pipeline.

**Next: commit.** User approved committing the full fix (package + paper + tests + review
reports + session notes). See git log for the commit.
