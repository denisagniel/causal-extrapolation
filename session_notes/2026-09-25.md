# Session Notes — 2026-09-25

## (untimed, pre-existing) — paper-protocol registries backfill

Not logged by whoever ran it; reconstructed from file contents at the start of the session
below for continuity. Before this session began, `inst/paper/claims.md`, `notation.md`, and
`outline.md` (main.tex-grounded) were created as a paper-protocol registries backfill — each
seeded by reading `main.tex` directly. A second, independent outline,
`outline-from-story.md`, was also drafted directly from `story.md` alone (not from `main.tex`),
explicitly as a from-first-principles comparison artifact, not merged with the main.tex-grounded
outline. `story.md` itself picked up a reconciliation note the same day (Path-3 two-regime EIF
rewrite; verdict: S5/S6 unchanged). None of this was committed or logged before the session below
started.

## 11:00 — story-first restructuring: promote outline, redraft all 10 sections

**Goal.** User: "I like the new outline from story and want to promote that and we'll start by
drafting the paper from scratch from that." Promote `outline-from-story.md` to the canonical
`outline.md`, then restructure `main.tex` into its 10-section story-first plan, reusing (not
re-deriving) the already-proven math.

**What happened.**

### Outline promotion

Archived the prior main.tex-grounded `outline.md` untouched at
`outline-superseded-2026-09-25.md`. Promoted `outline-from-story.md`'s content into `outline.md`
(deleted the now-redundant `outline-from-story.md`), updating its header to record the
promotion and adding a "what drafting from scratch means in practice" note: the manuscript's
already-proven propositions/lemmas/EIFs are not re-derived, only redistributed into new section
boundaries per `story.md`'s narrative. Added a content-migration table (§2) tracking, per new
section, what superseded-`main.tex` material it reuses and its drafting status — updated after
every subsequent edit rather than batched at the end.

### `main.tex` restructured into all 10 sections

Reused all previously-audited math verbatim (Propositions 1–3, Lemmas 1–2 and their proofs, all
three paths' EIFs, the asymptotic-normality proposition, every regularity condition) — nothing
re-derived, nothing re-audited as new. Changes were to section boundaries, titles, ordering, and
connective prose only, section by section against the promoted outline:

- **§1 Introduction** — kept the audited Fundamental-Promise/tension prose verbatim; added a new
  "estimand gap, not an estimation gap" paragraph and made the organizing move (choose what is
  held invariant) its own explicit paragraph; rewrote the contribution paragraph anchored to the
  new section labels.
- **§2 The Future Effect Family and Its Identification Problem** — merged the old Setting/context
  and Estimands sections into one, two subsections (`Setting`, `The future effect family`);
  strengthened the non-identification statement to "not merely unobserved but wholly unknown"
  (matching `story.md` S1's exact language); removed two dead commented-out paragraph blocks.
- **§3 Effect-Level Invariance: Weakening and Strengthening Dynamics** — merged Cohort structure
  (Lemmas 1–2) + Path 1 + the two "connecting" subsections into one section, two subsections,
  **reordered** so weakening (within-group homogeneity) precedes strengthening (collapse to the
  ATT), matching S2-then-S3. Removed the stale `θ_gt=f(g,I_t;θ)` "general framework" device that
  `main.tex`'s own header comment had flagged SEMI-DEPRECATED for Path 3 since a prior session —
  resolved that long-standing flag rather than migrating it forward.
- **§4 A Parametric Time Path** and **§5 Covariate Transport** — promoted Path 2 and Path 3 from
  subsections to top-level sections by wrapping each in a new `\section{}` immediately before the
  unchanged `\subsection{Path N...}`, deliberately avoiding touching the internal
  `\subsubsection{Choice of f...}` in either case (a direct rename would have orphaned that
  heading level; the wrapper approach has zero orphan-heading risk).
- **§6 Inference: Propagated, Not Re-Derived** — retitled/re-labeled (kept `\label{sec:eif}`
  alongside the new `\label{sec:inference}`, since dozens of appendix proof cross-references
  depend on the former); rewrote the intro paragraph to state the propagate-vs-not-propagate
  asymmetry as the section's organizing point; **relocated Path 3's two-regime EIF derivation
  here from §5**, as a third `\paragraph` alongside Path 1's and Path 2's, so the asymmetry is
  argued once across all three paths rather than split across two sections.
- **§7 Selecting a Temporal Model by Extrapolation, Not Fit** — promoted from a subsection to a
  top-level section (no internal `\subsubsection`, zero orphan-heading risk).
- **§8 Simulation Evidence** — retitled with `\label{sec:simulations}` added; **pulled the Path-2
  misspecification check in from the Additional-Simulations appendix**, since it is the direct
  evidentiary anchor for S4's "converges to the wrong number" claim (the outline's stated
  rationale for this specific pull). Left in the appendix, deliberately: Path-1 isolation, the
  synthetic-first-stage EIF-coverage duplicate of the already-in-body real-first-stage check,
  cohort-weights robustness, three stress tests, and CV robustness — none tied to a numbered
  claim the way the misspecification check is, so they remain genuine supplementary detail.
- **§9 Application** — already had its own `\section{}` and `\label{sec:application}`
  (discovered while resolving the Introduction's forward reference); needed no edits.
- **§10 Discussion** — redrafted: strengthened the opening synthesis into an explicit "one
  framework, three invariance choices" statement tying back to the Introduction's organizing
  move; added a new paragraph explicitly reconciling the application's failure result against
  that structural picture (not previously mentioned in the Discussion at all); expanded the
  scope-boundary restatements to include the no-first-stage-identification-strategy and
  covariates-unaffected-by-policy limitations per the outline's ownership plan for this row.

### Registries kept current in the same turns as the prose (paper-sequencing-gate hard gate)

- `claims.md` — C1/C3 re-anchored to the (now-existing) new sections; table 2 fully re-anchored
  to post-restructuring line numbers; added a supporting-evidence note on Proposition 2's row
  pointing at the relocated misspecification check; verified and resolved a previously-open item
  (C7's anchor in `section9_application.tex`, read for the first time this session).
- `notation.md` — added an explicit staleness flag: labels remain reliable, but the ~50 raw
  `main.tex:NNN` line-number citations were not individually re-verified this pass (unlike
  `claims.md`'s table 2, which was) — a proportionate tradeoff recorded rather than silently
  taken.
- `blessings.md` — did not exist; created. Every touched region logged `unblessed` (never
  `blessed` — that is the author's call, not this session's).

### Verification

Ran `compile-latex` (3×xelatex + bibtex) after every structural change, not just once at the end.
Final state: 60-page PDF, zero "Label(s) may have changed," zero new overfull hboxes, only the
two pre-existing missing-`.bib`-entry citation warnings (`tibshiraniExactPostselectionInference2016`,
`chenValidInferenceModel2021` — present before this session, still outstanding, not blocking).

**Decisions.** User: promote the story-first outline and draft from scratch. Two initially-deferred
items (Path 3 EIF → §6, misspecification → §8 body) were flagged rather than silently skipped
when first identified, then completed later in the same session under continued todo-list
pressure rather than left open.

## Next

- Score-matrix architecture (Item 2 — DiD score for staggered adoption; `R/build_omega_score.R`,
  `build_score_matrix.R`, `estimate_score_cov.R`, plus the `aggregate_groups.R`/
  `path1_aggregate.R` estimated-omega-variance changes) is separate, pre-existing uncommitted
  work from earlier the same day, paused (not abandoned) when the user pivoted to the paper
  restructuring — see this file's untimed entry above and `session_notes/2026-09-24.md`'s Item-2
  note for the theory context. No combining/GMM function over the score matrix exists yet; that
  remains the actual point of Item 2 and is still undone.
- The two missing `.bib` entries remain outstanding, unrelated to this session, not blocking.
- `main-lean.tex`/`theory.tex` archival remains deferred (per 2026-09-24's "let's wait" — the
  same file inventory issue persists: neither superseded skeleton was touched by this
  restructuring).
- `outline-superseded-2026-09-25.md` and the deleted `outline-from-story.md`'s content (now
  folded into the live `outline.md`) are the historical record of this restructuring's rationale;
  not intended for further edits.
- A stray file, `inst/paper/quality_reports/session_logs/README.md`, was found during this
  session referencing a *different* project (`missing-data-did`) — flagged as an apparent
  accidental copy, left untouched and uncommitted pending the user's confirmation it should be
  removed.
- Several other untracked files predate this session and are unrelated to it: two older,
  never-committed session notes (`2026-08-25.md`, `2026-09-02.md`) and an old dated plan
  (`quality_reports/plans/2026-08-03_sim-improvements-real-firststage-estimated-cate.md`) — left
  alone, not part of this commit.
