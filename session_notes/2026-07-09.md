# Session Notes — 2026-07-09

## Summary
Phase 3A: rewrote the SYG/firearm-homicide application (scripts 01–08) to call the
extrapolateATT package for Paths 1–2 with EIF-based inference, replacing code that bypassed
the package (audit C3). Added reproducibility scaffold (seeds, fs::dir_create, README,
run_all.R). Path 3 deferred pending the DiD transport influence function.

## Key results (real, EIF-based)
- Path 1 (constant): MSPE 2.51, coverage 0%. Path 2 (linear, CV-selected): MSPE 1.47,
  coverage 0%. Both underpredict — realized SYG effect accelerates to ~2.0–2.25 by 2020–22.
- This is the honest "promise and limits" story (audit M7); the lean paper's fabricated
  table + "Path 3 best" narrative (main-lean.tex 682–731) must be replaced in Phase 4.

## Reviewer pass caught 2 criticals (fixed)
- C2 (material): realized validation target was unweighted while predictions were ω-weighted
  → estimand mismatch, fixed by ω-weighting the target. Changed the numbers and the story.
- C1 (consistency): center=FALSE to match package CV variance convention (numerically nil).

## Decisions
- Path 3 in the application requires conditional DR-DiD transport EIF (gated in package) →
  DEFERRED. This session = Paths 1–2 + checkpoint (per user).
- Deferred polish: readr::write_rds, cat→message, randplot theme (style, non-blocking).

## Next
- Phase 3B: derive + validate DiD transport EIF, un-gate integrate_cate(design="did"),
  then rewrite application Path 3 (script 05).
- Phase 4: wire real section9 tables into paper §9; replace fabricated results/narrative.

## Phase 4 (partial): real §9 wired into lean paper
Replaced fabricated application table + false "Path 3 best" narrative in main-lean.tex §9
with pipeline-generated tables (\input section9_validation/yearly) and the honest story:
Path 2 > Path 1 but both underpredict the post-2015 acceleration; Path 3 deferred.
domain-reviewer SOUND (0 critical); fixed a background factual error (30 states 1995-2022,
not ">25 in 2005-2015"). Compiles clean (24pp, 0 errors).

## Commits (all merged to main)
- Phase 3A (application Paths 1–2): `b88ecdb`, merged `f98902b`.
- Phase 4 §9 wiring: `24f0c87`, merged `57943c2`.
Working tree clean except untracked build artifacts (inst/paper/main*.aux/.log/.pdf/.bbl,
.claude/projects/) — intentionally not committed.

## Next session: Phase 3B (DiD transport EIF) — start here
Goal: derive + validate the conditional DR-DiD transport influence function, un-gate
`integrate_cate(design="did")`, then complete the application Path 3 (script 05) and the
paper's deferred Path 3 column.
- **Theory first (linchpin):** the current `score_drdid()` is a NOT-validated placeholder
  (not mean-zero; missing Sant'Anna–Zhao propensity normalization). See
  `R/score_builders.R` (marked experimental) and the gate in `R/integrate_cate.R`
  (`design=="did"` errors). Write the correct DR-DiD transport EIF in a theory note first
  (parallel to the 2026-06-30 Path-3 note), cross-check numerically vs `drdid::drdid()`.
- **Then package:** replace `score_drdid`, remove the gate, add a DiD collapse/known-ATT
  test (not the old tautological one).
- **Then application:** rewrite `application/scripts/05` to a conditional DR-DiD CATE +
  `integrate_cate(design="did")`; feed 06/07 so the Path 3 column fills in.
- **Then paper:** replace the "pending" Path 3 row in `inst/paper/sim_tables/section9_*.tex`
  (regenerated by script 07) and update §9 prose (main-lean.tex ~671, 686-693) to include Path 3.
- Relevant memories: [[phase2-path3-direct-cate-package]], [[phase3a-application-paths12-done]],
  [[test-dr-estimators-with-misspecified-nuisances]] (test DiD with misspecified nuisances too).

## Phase 3B started (this session)
Branch `phase3b-did-transport`. Plan: ~/.claude/plans/toasty-wondering-stardust.md.
**Insight:** conditional DR-DiD = conditional unconfoundedness on the outcome change ΔY, so
score_drdid = A*(dY-m1)/e - (1-A)*(dY-m0)/(1-e) with m1=m0_dY+tau — no new contract fields.
Collapse to AIPW-on-changes EIF verified to machine precision. DRDID cross-check tests point
estimate only (Form A hard weight vs SZ smooth e/E[A] → different SE by construction).
Decisions: application uses parametric + causal-forest (compare); §8 sim fidelity gap fixed
too (sim_section7 still calls removed integrate_covariates()).

## Phase 3B COMPLETE (end of session)
All four parts done on branch `phase3b-did-transport`:
- **Package:** score_drdid() = two-arm AIPW-on-ΔY (m1=m0_dY+tau); DiD gate removed; DiD test
  suite (collapse/misspec/DRDID cross-check) added; DRDID in Suggests. 635 tests pass.
- **Sims:** rewired sim_section7 to integrate_cate() (was calling removed integrate_covariates);
  fixed a set.seed collision (Path 3 now bias -0.003/cover 95%); fixed write_paper_tables.R
  to write canonical inst/paper/ (was legacy latex/).
- **Application:** Path 3 column filled — conditional-DiD collapse, parametric + causal-forest,
  per-year density-ratio transport. Honest result: **all three paths underpredict**; Path 3
  MSPE 3.14/cover 14% (worst), Path 2 best (1.47). n_eff 8-36/43 = overlap limit. Pipeline
  01-08 reproducible.
- **Paper:** lean §9 + Promise/Limits + Appendix E pointer updated; compiles clean (24pp).
- **Reviews:** verifier all PASS + fidelity confirmed; r-reviewer criticals/majors fixed
  (seed, trimming disclosure, overlap diagnostic, fs::dir_create). Style-debt batch deferred.
- **Deferred:** full main.tex Appendix B DR-DiD write-up; sim_section8_2 (also old API, not in
  pipeline); application style batch (readr/theme_rand/message).

## Lighter Phase 4 remainders (independent of 3B, can do anytime)
- SYG background citation: `\citep{[CITATIONS]}` TODO in §9 background (main-lean.tex ~649) —
  needs a real firearm-policy reference; do NOT fabricate.
- Appendix E application details; full-paper proofread/polish; Phase 5 fidelity + review.

## Phase 3B deferred items (branch `phase3b-deferred`, plan humble-kindling-unicorn.md)
Clearing all three deferred items in one pass:
1. **§8.2 sim** — rewrite `sim_section8_2_misspec.R` to unit-level `integrate_cate()` (misspec
   vs oracle, `cor_XU∈{0,0.5,0.8}`), delete `_v2` stub, wire into `run_section8.R`, add
   `section8_2.tex` + fix `write_section8_tables.R` legacy-dir bug (→ `inst/paper/sim_tables`).
2. **DR-DiD derivation** — add explicit conditional DR-DiD transport score to `main.tex`
   `app:eif3` (match `score_drdid`: AIPW-on-ΔY, `m1=m0+τ`) + compact inline mirror in canonical
   `main-lean.tex`. `main-lean` has no appendix (dangling refs pre-date this).
3. **Style batch** — `readr::write_rds`, `cat`→`message`, `theme_rand` across 9 app scripts.

### Phase 3B deferred — COMPLETE (all 3 done, verified)
- **§8.2 sim:** rewrote to unit-level `integrate_cate` (unobserved-modifier U corr X; X-only vs
  oracle). 1000 reps: misspec bias 0.001/0.748/1.205 for cor∈{0,.5,.8} (= analytic β_U·ρ·μ),
  coverage 95%→0.7%→0%; oracle unbiased ~95%. Deleted `_v2` stub; wired into `run_section8.R`;
  added `section8_2.tex`; fixed `write_section8_tables.R` legacy out_dir → `inst/paper/sim_tables`.
  Also fixed pre-existing `load_all("package")` blocker in sim_section8_1/8_3.
- **DR-DiD derivation:** explicit derivation in `main.tex` app:eif3 (parallel-trends =
  unconfoundedness-on-ΔY; score (∗∗) matches `score_drdid` exactly) + inline mirror in
  `main-lean.tex`. domain-reviewer SOUND; applied its 2 MAJOR fixes (strict-overlap condition;
  DiD nuisances (m_0,e,w) w/ τ fixed) + collapse SE caveat. Both compile clean (47pp / 24pp).
- **Style batch:** all 9 app scripts (readr/message/theme_rand); `run_all.R` reproduces clean.
- **Reviews:** r-reviewer no Critical/Major; domain-reviewer SOUND + fixes applied; verifier
  5/5 PASS. Branch `phase3b-deferred`. NOT yet committed/merged (awaiting user).
- **Out of scope (unchanged):** §8.2/Appendix D `\input` into paper (needs appendix); SYG
  `[CITATIONS]`; pre-existing `09_policy_comparison.R` `cap` data bug (fails on main too).
