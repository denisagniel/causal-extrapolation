# Session Notes — 2026-06-30

## Summary

Resumed after long gap. Committed all accumulated work (application pipeline, lean paper draft, session artifacts) as `6d09ea4`, then turned to a structural re-evaluation of the lean paper, which surfaced a Path 3 identification issue and led to a decision to audit the whole paper.

## Key Decisions

- **Keep "Fundamental Promise of Causal Inference" framing** in the intro (user reintroduced it deliberately). Align the abstract TO the intro, not the reverse — supersedes the 2026-05-27 plan's recommendation to cut it. (memory: keep-fundamental-promise-framing)
- **Paper contribution = mapping, not estimation.** All three paths plug an existing asymptotically linear first-stage estimator into a forward-looking-estimand map + propagate its EIF. No new baseline estimation (group-time or CATE). Keep all three paths parallel in weight. (memory: paper-contribution-is-mapping-not-estimation)
- **Path 3 reframed: estimate CATE τ(x) directly from microdata**, integrate over the future covariate distribution F_X^{p+m}. Overrides committed code (`integrate_covariates.R` inverts β from marginal θ_gt) and full-paper Prop 3. Fixes the lean skeleton's missing-future-time-index bug and removes the injectivity assumption. Conditional analogs of both first-stage designs (unconfoundedness → CATE/DR-learner; DiD → conditional DR-DiD). DEFERRED into the audit — not edited in isolation. (memory: path3-direct-cate-estimation)

## In Progress

- **Full-paper audit** (user: treat everything as provisional). Scope: full `main.tex` as source of truth + lean structure checked against corrected substance. Deep adversarial multi-agent (proof-auditor, domain-reviewer, r-reviewer, structure-reviewer, verifier). Deliverable: prioritized findings doc + proposed fix plan in quality_reports/, NO edits until approved.

## Audit Complete

- 5-agent adversarial audit done. Findings cross-confirmed (3–4 agents agreed on each critical). Report: `quality_reports/2026-06-30_full-paper-audit.md`; plan: `quality_reports/plans/2026-06-30_audit-remediation-plan.md`.
- Top criticals: C1 Path 3 invert-from-marginals is wrong/vacuous (validates the reframe); C2 `≈`-in-proof; C3 application bypasses the package entirely (EIF claims false as run); C5 compile errors in main.tex.

## Phase 0 — Framing Locked (2026-06-30)

- **Estimands:** FATT + FATU + FATE as one identified family (direct-CATE reframe unifies them); FITE/FATS = motivation only. Revises earlier "cut to FATT+FATE" — FATU retained (adopt decision).
- **Title:** "Should We Keep the Policy? Forward-Looking Estimands and Identification for Panel Data."
- **Abstract↔intro:** align abstract up to Fundamental Promise framing (keep intro).
- **Unifying abstraction:** plug existing asymptotically-linear estimator → forward-estimand map → propagate EIF (θ_gt for Paths 1–2, τ(x) for Path 3).
- Recorded in memory: paper-framing-decisions.

## Phase 1 — Math APPROVED (2026-06-30)

Theory note: `quality_reports/2026-06-30_phase1-path3-theory-note.md`.
- **Path 3 reframed:** estimand θ_{p+1}=∫τ(x)dF^{p+1}=E_src[w(X)τ(X)] (Prop 3'); assumptions A1 (nuisance rates), A2 (structural stability), A3 (bounded w), A2' (cross-group transportability — price of FATU/FATE only). DR transport EIF with collapse-to-AIPW/ATT certificate.
- **Key correction (DA caught):** CATE τ̂ is NOT √n with continuous X (rate n^{-β/(2β+d)}); √n recovered at the *functional* level via Neyman-orthogonal/DR score + cross-fitting. A1 stated as nuisance rates, not asymptotic linearity of τ̂. EIF is DR score, not w·φ_τ.
- **Scope decision (DA):** keep DML rate conditions at citation level (not the paper's point); overlap/bounded-w stays as the one substantive limit (intrinsic to transport) + one sim stress regime.
- **Paths 1–2 / theorem hygiene:** Prop 2 hidden assumption → hypothesis (M2); "efficiency bound" → "valid asymptotically-linear IF" (M3); complete asymptotic-normality theorem with Path 3 step (M5); scope lem:injectivity to Path 2.
- Consequence for Phase 2: package Path 3 needs outcome regressions + propensity (AIPW pieces), not just point CATE → wrap DoubleML/grf.

## Phase 1 LaTeX pass COMPLETE (main.tex)

Reframed Path 3 fully written in: §4.3 (Prop 3' transport, assumptions A1–A3 + A2'), §5 EIF (DR transport score), appendix proof (change-of-measure), app:eif3 (DR derivation + collapse lemma to AIPW/ATT), RC8–RC10 rewritten. Plus: Prop 2 hidden assumption promoted (M2); efficiency overclaims downgraded (M3); Path 3 step added to asymptotic-normality proof (M5); estimands § = FATT/FATU/FATE family (decision-mapped, shared machinery), FITE/FATS motivation only; `\lemma` dup + all 4 overfull hboxes + "withing" typo fixed (C5). Bib: added Chernozhukov + Sant'Anna-Zhao.

Verified: main.tex compiles exit 0, 0 undefined refs/citations, 0 overfull >10pt, 45 pages.

## Lean rewrite — started then re-scoped to structure eval

Committed main.tex Phase 1 (2242abc). Began lean rewrite (§1 framing: new title + abstract + related-work; §2–3: setting + FATT/FATU/FATE family; §4: "what is invariant" framing + Path 3 transport reframe). User stopped mid-§5 — wants to evaluate structure/outline, not write heavily now.

**Structural review written:** `quality_reports/2026-06-30_lean-structure-review.md`. Locked decisions (all my recs accepted):
1. §2.4 first-stage EIF → move to §5 (§2 = clean setup).
2. §4 framing → "what is assumed invariant" umbrella (effects/temporal-form/conditional-effect); `h(g,t;γ)` demoted to a Paths-1–2 device.
3. §4 length ≤6.5pp; paths ≤1.5pp each.
4. §6/§7 → placeholder tables, write empirical prose after Phase 3.

Updated `inst/paper/outline-lean.md` with a SUPERSEDED banner pointing to current decisions.

## State of main-lean.tex
§1–4 content rewritten (new title/abstract, FATT/FATU/FATE, Path 3 transport). NOT yet executed: the §2.4→§5 move and the §4 "what is invariant" reframing (decisions 1–2 above). §5 partially old. §6/§7 still have fabricated numbers (need placeholdering). NOT yet compiled since edits. Uncommitted.

## Next Steps

- When resuming lean: execute decisions 1–2 (move §2.4 EIF to §5; reframe §4.1 around invariance), placeholder §6/§7 tables, compile-verify.
- **Phases 2–3** (package `integrate_cate` + application rewrite) still pending; §6/§7 real numbers depend on them.
- Uncommitted now: lean §1–4 edits, structure review, outline banner, session notes/log.
